# Cursor Rules

This is a small TypeScript app. Use TypeScript and keep the code tidy.

- Write clean, readable code
- Follow the style already in the file you're editing
- Prefer a functional style where it makes sense
- Add a comment for anything non-obvious

When you change something, explain what you changed and why.

Don't break the existing tests.
